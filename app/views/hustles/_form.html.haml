= form_for @hustle do |f|
  - if @hustle.errors.any?
    #error_explanation
      %h2= "#{pluralize(@hustle.errors.count, "error")} prohibited this hustle from being saved:"
      %ul
        - @hustle.errors.full_messages.each do |msg|
          %li= msg

#  .field
#    = f.label :user
#    = f.text_field :user
  .field
    = f.label :name
    = f.text_field :name
#  .field
#    = f.label :created
#    = f.datetime_select :created
#  .field
#    = f.label :modified
#    = f.datetime_select :modified
  .actions
    = f.submit 'Save'
